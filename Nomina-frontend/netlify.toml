[build]
  command = "npm ci && npm run build:src"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback (React Router, etc.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
